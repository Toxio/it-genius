/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['framerusercontent.com'], // Add your external image domain here
  },
}

export default nextConfig;
